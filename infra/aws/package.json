{
  "name": "@logs-explorer/aws",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "synth": "pnpm run build && cdk synth",
    "diff": "pnpm run build && cdk diff",
    "deploy": "pnpm run build && cdk deploy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.245.0",
    "constructs": "^10.6.0",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "aws-cdk": "^2.1114.1",
    "typescript": "catalog:"
  }
}
